{
  "name": "openlens-streamdeck",
  "version": "0.1.0",
  "private": true,
  "description": "A plugin for OpenDeck and the Elgato Stream Deck, so hardware keys can switch OpenLens scenes, pause the camera and drive key lights.",
  "type": "module",
  "scripts": {
    "sync": "./sync.sh",
    "pretest": "./sync.sh",
    "test": "node test/manifest.js && node test/deck-socket.js && node test/smoke.js",
    "install-plugin": "./install.sh"
  },
  "license": "MIT"
}

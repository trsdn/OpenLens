<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>OpenLens Camera</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>SYSX</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>14.0</string>
	<key>NSSystemExtensionUsageDescription</key>
	<string>OpenLens installs a camera extension so conferencing apps can use your processed video.</string>
	<key>CMIOExtension</key>
	<dict>
		<!-- The team prefix is spelled out rather than taken from
		     $(TeamIdentifierPrefix), which is only defined when the build signs
		     with a team. The notarization broker builds the source unsigned and
		     signs afterwards, so the variable expands to nothing there and the
		     name loses its prefix. sysextd then refuses the extension with
		     "invalid mach service name ... must be prefixed with one of the App
		     Groups in the entitlement" and uninstalls it on the spot, which
		     looks like "extension category returned error" in the app. -->
		<key>CMIOExtensionMachServiceName</key>
		<string>G69Z5BNY97.$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	</dict>
</dict>
</plist>
